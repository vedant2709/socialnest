<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mini Project 3</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <div class="w-full min-h-screen bg-zinc-900 px-10 py-5 text-white">
    <div class="flex w-full justify-end">
      <a href="/logout" class="bg-red-500 px-3 py-1 font-semibold text-md rounded-md hover:bg-red-600">Logout</a>
    </div>
    <h5 class="mb-5">Edit your post</h5>
    <form action="/update/<%= post._id %>" method="post">
      <textarea class="resize-none bg-transparent border-2 border-zinc-600 rounded-md w-1/3  block outline-none" name="content">
        <%= post.content %>
      </textarea>
      <input type="submit" class="px-3 py-1 bg-yellow-500 text-black rounded-md w-36 text-lg font-semibold block mt-3 hover:bg-yellow-600 cursor-pointer" value="Update Post">
    </form>
  </div>
</body>
</html>